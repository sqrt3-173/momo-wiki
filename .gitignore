# OS / editor cruft
.DS_Store
*.swp
*~

# Obsidian per-machine UI state (config can sync; workspace/cache shouldn't)
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache
.trash/

# Secrets — never commit credentials into the wiki
*.token
*.key
*.pem
.env
.env.*
secrets/
*-token
*-secret*
